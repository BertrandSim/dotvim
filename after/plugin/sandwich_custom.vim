
" keep sandwich default mappings, except
" except sdb/srb.
" Instead, change implementation, by adding a recipe with 'b' (or another key) as input
" see ../../plugin/sandwich_custom.vim
unmap sdb
unmap srb
